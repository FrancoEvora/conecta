"use client";
import { useState } from "react";
import { Icon, NetworkMark } from "@/components/UI";
export default function LoginForm(){const [state,setState]=useState({status:"idle",message:""});async function submit(e){e.preventDefault();setState({status:"loading",message:""});const form=new FormData(e.currentTarget);const response=await fetch("/api/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(form.entries()))});const data=await response.json();if(!response.ok){setState({status:"error",message:data.error||"Credenciais inválidas."});return;}sessionStorage.setItem("conecta-session",JSON.stringify(data.session));location.href="/demo";}return <form className="login-form form" onSubmit={submit}><NetworkMark/><h2>Entrar na Rede Conecta</h2><p>Use as credenciais cadastradas pela administração.</p><label>E-mail<input name="email" type="email" required/></label><label>Senha<input name="password" type="password" required minLength="6"/></label><button className="button button--orange button--block" disabled={state.status==="loading"}>{state.status==="loading"?"Entrando…":"Entrar"}<Icon name="arrow"/></button>{state.message&&<p className="form-error">{state.message}</p>}<small>O provisionamento de usuários é realizado pelo administrador da organização.</small></form>}
